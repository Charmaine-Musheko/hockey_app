import 'package:flutter/material.dart';

// Define custom colors for the app's new sleek theme
class AppColors {
  // Primary dark green background color
  static const Color primaryGreen = Color(0xFF1A2B51); // A deep, dark green

  // Lighter green for accents or secondary elements
  static const Color secondaryGreen = Color(0xFF85C9E6); // A brighter green

  // Orange/Gold accent color
  static const Color accentOrange = Color(0xFFD8B96A); // A vibrant orange/gold

  // White for content backgrounds and text contrast
  static const Color white = Colors.white;

  // Dark text color for readability on light backgrounds
  static const Color darkText = Color(0xFF333333); // A dark grey for text
}